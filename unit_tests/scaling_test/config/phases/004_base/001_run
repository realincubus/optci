#!/bin/bash
OPTCI_PREPARE

time="$(OPTCI_RUN numactl -N $SOCKETS -C +$CORES_PER_SOCKET ./bin_matrix_multiply_example $SIZE $ITERATIONS)"

CONFIG_TUPLE=$SOCKETS''_$CORES_PER_SOCKET

echo "export BASE_RUNTIME_$CONFIG_TUPLE=$time" > $ARTIFACT/base_runtime_$CONFIG_TUPLE

OPTCI_CLEANUP
